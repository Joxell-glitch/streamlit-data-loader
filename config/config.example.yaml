network: mainnet
api:
  rest_base: "https://api.hyperliquid.xyz"
  info_path: "/info"
  websocket_url: "wss://api.hyperliquid.xyz/ws"
  testnet_rest_base: "https://api.hyperliquid-testnet.xyz"
  testnet_websocket_url: "wss://api.hyperliquid-testnet.xyz/ws"

trading:
  quote_asset: "USDC"
  initial_quote_balance: 10000.0
  min_position_size: 50.0
  max_position_size: 500.0
  min_edge_threshold: 0.001
  safety_slippage_buffer: 0.0005
  max_concurrent_triangles: 5
  top_n_opportunities: 20
  whitelist: []
  blacklist: []
  min_average_volume: 0
  max_spread_pct: 0.01

database:
  backend: sqlite
  sqlite_path: "data/arb_bot.sqlite"
  postgres:
    host: "localhost"
    port: 5432
    user: "user"
    password: "password"
    database: "hyperliquid"

logging:
  level: INFO
  log_file: "data/bot.log"
  console: true

feed_health:
  log_interval_sec: 1
  stale_ms: 1500
  out_of_sync_ms: 1000
  dedup_ttl_sec: 2

observability:
  log_top_n_each_sec: 60
  top_n: 10
  min_abs_profit_to_log: 0.0

validation:
  enabled: false
  sample_interval_ms: 250
  stats_log_interval_sec: 5
  sqlite_flush_every_n: 50
